Public Class Login
    Inherits Page

    Protected Sub Page_Load(sender As Object, e As EventArgs) Handles Me.Load
        ' En esta rama solo se prepara la estructura visual.
        ' La autenticación contra SQL Server se implementará en feature/login.
    End Sub
End Class